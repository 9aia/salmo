# TODO

- salmo install --all; salmo i -a
- salmo install --packages; salmo i -p
- salmo install --files; salmo i -f

- Add pull command (files using salmoinclude file)
- Add support for root mode and user mode (like running a folder/repo for a specific user)
- Release
